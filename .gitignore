# Environment normalisation:
/.bundle
/vendor/bundle
# NOTE: these should all be checked in to normalise the environment:
# Gemfile.lock, .ruby-version, .ruby-gemset

# Ignore Bundler dependency graphs. Can be updated as needed with `bundle viz`
gem_graph.png

# Ignore all logfiles and tempfiles.
/log/*
!/log/.keep
/tmp
/spec/tmp

# ignorable testing files
.byebug_history
capybara-*.html
.rspec-local
/coverage/

# ignore secrets
config/initializers/secret_token.rb

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# Ignore dev/test dotenv environment settings
.env
.env.*
# don't ignore .env.example
!.env.example

# Editor Files & Dirs
/.idea
/.vscode
*.sublime-*


# Packages & Archives
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
